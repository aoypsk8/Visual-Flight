import '../utils/app_resources.dart';

/// ชื่อและโลโก้แอป — ใช้ร่วมกันทั้ง UI และ platform label
final class AppBranding {
  AppBranding._();

  static const String appName = 'FoFuckDingo';
  static const String logoAsset = AppIcons.dingoCoin;
}
